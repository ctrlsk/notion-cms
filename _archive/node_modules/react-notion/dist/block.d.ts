import * as React from "react";
import { DecorationType, BlockType, BlockMapType, MapPageUrl, MapImageUrl } from "./types";
export declare const renderChildText: (properties: DecorationType[]) => JSX.Element[];
interface Block {
    block: BlockType;
    level: number;
    blockMap: BlockMapType;
    mapPageUrl: MapPageUrl;
    mapImageUrl: MapImageUrl;
    fullPage?: boolean;
    hideHeader?: boolean;
}
export declare const Block: React.FC<Block>;
export {};
